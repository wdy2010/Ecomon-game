# 에코몬 성장 게임 미션

미션을 순서대로 수행하세요.

---

## 미션 0. 고장 난 게임 확인하기

1. Codespace에서 터미널을 엽니다.
2. 다음 명령을 입력합니다.

```bash
npm start
```

3. 3000번 포트의 웹페이지를 엽니다.
4. 환경 행동 버튼을 눌러 봅니다.
5. 추천 미션 버튼도 눌러 봅니다.

### 성공 조건

- [ ] 게임 화면이 정상적으로 열렸다.
- [ ] 환경 행동 버튼이 아직 작동하지 않는 것을 확인했다.
- [ ] 추천 미션 버튼이 작동한다.
- [ ] 초기 점수가 0점이다.

---

## 미션 1. 나만의 에코몬 만들기

`index.html`을 열고 다음 중 두 가지 이상을 수정하세요.

- 게임 제목
- 게임 소개 문장
- 환경 실천 행동 이름
- 행동 버튼의 이모지
- 하단 안내 문구

예시:

```html
<h1>초록별 수호대</h1>
```

`app.js`의 `stages`에서 다음도 수정할 수 있습니다.

- 단계별 에코몬 이름
- 단계별 이모지
- 단계별 안내 문장

예시:

```javascript
{
  minScore: 20,
  name: "새싹 용사 에코몬",
  emoji: "🌱",
  label: "2단계",
  message: "새싹 용사가 모험을 시작했어요!"
}
```

### 주의

`minScore`는 아직 변경하지 않는 것이 좋습니다.

### 성공 조건

- [ ] 게임 제목이나 소개 문장을 변경했다.
- [ ] 에코몬 이름 또는 이모지를 변경했다.
- [ ] 새로고침 후 변경 결과를 확인했다.

### 첫 번째 커밋

```bash
git add .
git commit -m "게임 제목과 에코몬 꾸미기"
git push
```

---

## 미션 2. 게임 색상 변경하기

`styles.css` 위쪽의 `:root`를 찾아 색상을 변경하세요.

예시:

```css
--primary-color: #7c3aed;
--primary-dark: #4c1d95;
--primary-light: #ede9fe;
--accent-color: #fbbf24;
--background-color: #f5f3ff;
```

다음 중 두 가지 이상을 변경합니다.

- 기본 색상
- 배경색
- 강조색
- 카드의 둥근 정도
- 버튼의 색상

### 성공 조건

- [ ] 기본 색상을 변경했다.
- [ ] 배경색 또는 강조색을 변경했다.
- [ ] 글자와 배경이 잘 구분된다.
- [ ] 버튼의 글자를 읽을 수 있다.

---

## 미션 3. 환경 행동 기능 완성하기

`app.js`에서 다음 코드를 찾으세요.

```javascript
actionButtons.forEach(function (button) {
```

`TODO` 부분에 다음 기능을 구현합니다.

1. 클릭한 버튼의 행동 이름을 가져온다.
2. 클릭한 버튼의 점수를 숫자로 가져온다.
3. 행동 전 진화 단계를 저장한다.
4. 에코 점수를 증가시킨다.
5. 실천 횟수를 증가시킨다.
6. 실천 기록을 배열 맨 앞에 추가한다.
7. 화면을 다시 그린다.
8. 완료 메시지를 표시한다.
9. 진화 여부에 따라 애니메이션을 실행한다.

### 필요한 값 가져오기

클릭한 버튼은 이벤트 함수 안의 `button`입니다.

```javascript
const action = button.dataset.action;
const points = Number(button.dataset.points);
```

현재 단계 번호는 다음과 같이 가져올 수 있습니다.

```javascript
const previousStageIndex = getCurrentStageIndex();
```

배열 맨 앞에 기록을 추가하는 방법은 다음과 같습니다.

```javascript
records.unshift({
  action: action,
  points: points
});
```

점수를 변경하고 `render()`를 실행한 뒤 현재 단계를 다시 확인합니다.

```javascript
const currentStageIndex = getCurrentStageIndex();
```

### AI 요청 조건

- `app.js`의 TODO 부분만 수정하게 합니다.
- 전체 파일을 다시 작성하게 하지 않습니다.
- `server.js`를 수정하지 않습니다.
- 먼저 변경 계획을 설명하게 합니다.
- 수정 후 테스트 방법도 요청합니다.

### 성공 조건

- [ ] 텀블러 버튼을 누르면 10점 증가한다.
- [ ] 걷기 버튼을 누르면 20점 증가한다.
- [ ] 버튼을 누를 때마다 실천 횟수가 1 증가한다.
- [ ] 최근 행동이 기록 목록 위쪽에 나타난다.
- [ ] 점수가 증가하면 진행률 막대도 증가한다.
- [ ] 단계가 유지되면 에코몬이 점프한다.
- [ ] 단계가 바뀌면 진화 애니메이션이 실행된다.

### 두 번째 커밋

```bash
git add .
git commit -m "환경 행동과 에코몬 성장 기능 구현"
git push
```

---

## 미션 4. 경계값 테스트하기

에코몬은 다음 점수에서 진화합니다.

```text
20점, 40점, 70점, 100점
```

다음을 직접 확인하세요.

- 10점에서는 알 상태인가?
- 20점에서 새싹으로 변하는가?
- 40점에서 초록 에코몬으로 변하는가?
- 70점에서 나무 에코몬으로 변하는가?
- 100점에서 지구 수호 에코몬으로 변하는가?
- 100점을 넘겨도 진행률 막대가 화면을 벗어나지 않는가?

점수가 100점을 넘어가는 것은 허용됩니다. 진행률 막대만 100%를
넘지 않아야 합니다.

---

## 미션 5. 다른 학생의 게임 테스트하기

짝과 게임을 바꾸어 테스트하세요.

다음 형식으로 문제를 기록합니다.

```text
재현 순서:
1. 게임을 초기화한다.
2. 가까운 거리 걷기 버튼을 다섯 번 누른다.

실제 결과:
100점이 되었지만 에코몬이 진화하지 않는다.

기대 결과:
지구 수호 에코몬으로 진화해야 한다.

오류 메시지:
콘솔 메시지를 작성한다.
```

### 성공 조건

- [ ] 친구의 게임을 테스트했다.
- [ ] 정상 동작 또는 오류를 구체적으로 기록했다.
- [ ] 수정 후 같은 방법으로 다시 테스트했다.

---

## 미션 6. AI 활용 기록 작성하기

`AI_LOG.md`를 열고 다음 내용을 작성합니다.

- 해결하려고 한 문제
- AI에게 입력한 주요 요청
- AI가 수정한 코드
- 직접 확인한 테스트
- AI 코드에서 발견한 문제
- 직접 수정한 내용

---

# 선택 미션

필수 미션을 완료한 후 한 가지를 선택합니다.

---

## 선택 미션 A. 진화 축하 효과 추가

에코몬이 진화할 때 다음 중 하나를 추가하세요.

- 화면에 축하 문장 표시
- 배경색이 잠시 변경되는 효과
- 반짝이는 이모지 표시
- 진화한 단계 이름을 기록 목록에 추가

### 조건

- 단계가 바뀔 때만 실행되어야 합니다.
- 일반 행동에서는 실행되지 않아야 합니다.
- 외부 라이브러리를 사용하지 않습니다.

---

## 선택 미션 B. 연속 실천 콤보

3번 연속으로 행동하면 보너스 점수를 주는 기능을 만드세요.

예시:

```text
3회 연속 실천 달성! 보너스 +5점
```

### 테스트할 내용

- 1회와 2회에는 보너스가 없어야 한다.
- 3회에만 보너스가 적용되어야 한다.
- 실천 횟수와 점수가 올바르게 표시되어야 한다.
- 초기화하면 콤보도 0이 되어야 한다.

---

## 선택 미션 C. 새 환경 행동 추가

`index.html`에 새로운 행동 버튼을 추가하세요.

예시:

```html
<button
  class="action-button"
  type="button"
  data-action="이면지 사용하기"
  data-points="10"
>
  <span class="action-emoji" aria-hidden="true">📄</span>
  <span class="action-name">이면지 사용</span>
  <span class="action-points">+10점</span>
</button>
```

### 성공 조건

- [ ] 새 버튼이 화면에 나타난다.
- [ ] 새 버튼을 눌렀을 때 점수가 증가한다.
- [ ] 실천 기록에 올바른 이름이 나타난다.
- [ ] 추천 미션에도 새 행동이 등장할 수 있다.

---

## 선택 미션 D. 새로고침 후에도 기록 유지하기

`localStorage`를 사용해 게임 상태를 저장하세요.

저장할 값:

- `ecoScore`
- `actionCount`
- `records`

AI 요청 예시:

```text
현재 에코몬 게임은 새로고침하면 점수와 기록이 초기화돼.

localStorage를 사용해 ecoScore, actionCount, records를
저장하고 불러오고 싶어.

조건:
- app.js만 수정한다.
- 기존 기능을 삭제하지 않는다.
- 외부 라이브러리를 사용하지 않는다.
- 저장된 값이 없거나 잘못되어 있어도 오류가 나지 않아야 한다.
- 행동 후 자동으로 저장한다.
- 초기화 버튼을 누르면 저장된 데이터도 삭제한다.

먼저 필요한 함수와 실행 순서를 설명해 줘.
그다음 최소한의 코드 변경만 제안해 줘.
마지막에 테스트 방법을 알려 줘.
```

### 추가 테스트

- [ ] 점수를 얻은 뒤 새로고침해도 점수가 유지된다.
- [ ] 실천 횟수가 유지된다.
- [ ] 기록 목록이 유지된다.
- [ ] 초기화한 뒤 새로고침해도 0점이다.

---

## 최종 제출

```bash
git status
git add .
git commit -m "에코몬 성장 게임 완성"
git push
```

GitHub 저장소에서 최종 커밋이 보이는지 확인하세요.